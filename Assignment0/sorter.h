int main (int argc, char **argv);
char* trim(char* word);
int compare(char* str1, char* str2);
void up(char* str); 
int colCheck(char* str);
#ifndef MetaData_H
#define MetaData_H

typedef struct MetaData {
	char** data;
}p;

#endif

